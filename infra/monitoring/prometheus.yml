global:
  scrape_interval: 15s
rule_files: ['/etc/prometheus/alerts.yml']
scrape_configs:
  - job_name: agent-desktop
    metrics_path: /internal/metrics
    authorization:
      type: Bearer
      credentials_file: /run/secrets/ops-token
    static_configs:
      - targets: ['host.docker.internal:8000']
